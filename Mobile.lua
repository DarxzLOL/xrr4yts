local Mobile = {}

function Mobile:Enable(Window)
    -- Mobile optimization
end

function Mobile:SetScale(Window)
    -- Auto scale UI
end

function Mobile:EnableTouchDrag(Window)
    -- Touch drag
end

function Mobile:EnableSafeArea(Window)
    -- Safe area (notch)
end

return Mobile
